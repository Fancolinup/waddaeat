// Wrapper module to expose dialogues as JS for easier development.
// You can later replace the require below with a plain object literal to edit content directly.
// Example: module.exports = { workQuotes: [...], voteTopics: [...] };

const data = require('./Pilot dialogues.json');
module.exports = data;